# frozen_string_literal: true

module Console
  # Stands for the host's own Administrate base controller: authentication and authorization live
  # here, not in the engine.
  class ApplicationController < Administrate::ApplicationController
  end
end
